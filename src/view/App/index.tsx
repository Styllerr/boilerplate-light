import React, { FC } from 'react';

import { AppContainer } from './styles';

export const App: FC = () => {
    return (
        <AppContainer>
            <h1>Test BP</h1>
        </AppContainer>

    );
};
